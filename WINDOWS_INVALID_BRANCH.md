# Windows Invalid Branch

This branch intentionally contains `<` and `>` in its name.

It is used only to verify that Windows Git fetch fails before running the guard
and succeeds after adding a local negative refspec.
